#! /bin/bash

# > doesn't allow to include the previous text
# >> allows the previous test to be concatincated'

#Having a text being saved into a file
echo "Redirecitng this text to a file" >> file.txt

#Having a user enter text and then being saved into a file
cat >> file.txt